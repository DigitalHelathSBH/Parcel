DB_DRIVER=ODBC Driver 17 for SQL Server
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=
DB_USER=
DB_PASSWORD=
# how SQL_CHAR (varchar) columns come back from the driver - "cp874" on Windows,
# but on Linux msodbcsql often already transcodes to "utf-8" before pyodbc sees it
DB_CHAR_ENCODING=cp874
